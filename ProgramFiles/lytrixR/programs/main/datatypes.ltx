void BackLine() {

    int a = 10;
    float b = 3.5;
    double c = 42;
    char d = 'X';
    string e = "Lytrix";

    writeSc("Initial values:");
    writeSc(a);
    writeSc(b);
    writeSc(c);
    writeSc(d);
    writeSc(e);

    a = 25;
    c = 7;

    writeSc("Updated values:");
    writeSc(a);
    writeSc(c);

    writeSc();

    return EOM;
}
